# Deny direct web access to the SQLite database and stored images
# (images are served through api.php?action=image)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
